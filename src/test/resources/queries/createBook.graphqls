mutation CreateBook($name: String!, $pageCount: Int!, $genre: Genre!) {
    createBook(bookInput:{name: $name, pageCount: $pageCount, genre: $genre}) {
        id
        name
        pageCount
    }
}
